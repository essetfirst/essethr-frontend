import React from "react";

// const createReducer = (handlers) => (state, action) => {
//   return handlers[action.type](state, action);
// };
const RemoteTable = ({ api }) => {
  return <div>Remote Table</div>;
};

export default RemoteTable;
