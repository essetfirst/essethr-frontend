# API origin only (no /api path). Used by src/setupProxy.js in development.
# API origin (Vite — preferred)
VITE_API_URL=http://localhost:4000

# Legacy CRA env (still supported)
# REACT_APP_API_URL=http://localhost:4000
GENERATE_SOURCEMAP=false
